package com.kodilla.stream.lambda;

public class ExpressionExecutor {
    public void executeExpression(int a, int b, MathExpression mathExpression) {
        ExpressionExecutor expressionExecutor = new ExpressionExecutor();

    }
}
